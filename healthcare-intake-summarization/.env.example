ENV=development
LOG_LEVEL=INFO

# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=healthcare_db

# Gemini API Key (Optional: system falls back to mock summary if key is not configured)
GEMINI_API_KEY=your_gemini_api_key_here
